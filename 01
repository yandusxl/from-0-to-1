#include<iostream>
using namespace std;
int main()
{
	double sum = 0;
	for (int j = 1;j < 101;j++)
	{
	
		double i = 1.0 / j;

		if (j % 2 != 0)
		{
			sum = sum + i;
		}
		if (j % 2 == 0)
		{
			sum = sum - i;
		}
		cout << i<< endl;
	}
	cout << sum << endl;
	system("pause");
	return 0;
}
